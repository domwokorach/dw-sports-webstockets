#include <nan.h>

    //gcstats method
    NAN_METHOD(AfterGC);
    NAN_GC_CALLBACK(afterGC);
    NAN_GC_CALLBACK(recordBeforeGC);
    
    //eventloop methods
    void reset();
    NAN_METHOD(read);
    void on_check(uv_check_t* handle);
