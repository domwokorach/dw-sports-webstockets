var stats = require("./lib/index");


var t = [];

setInterval(function () {
  for (let i = 0; i < 100; i++) {
    t.push(new Date());
  }
}, 10);

setInterval(function () {
  while (t.length > 0) {
    t.pop();
  }
});
setInterval(function () {
  stats.getStats();
}, 5000);
